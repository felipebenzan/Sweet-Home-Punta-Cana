# --- Instagram API Credentials ---
# Your numeric Instagram User ID for the Business or Creator account.
IG_USER_ID=YOUR_NUMERIC_IG_USER_ID

# A long-lived (60-day) User Access Token for the Instagram Graph API.
# See README.md for instructions on how to obtain this.
IG_ACCESS_TOKEN=YOUR_LONG_LIVED_USER_TOKEN

# The base URL for the Instagram API. Defaults to the Graph API.
# For Basic Display API, use: https://graph.instagram.com
IG_API_BASE=https://graph.instagram.com

# --- Function Configuration ---
# How long (in minutes) to cache Instagram data in Firestore before it's considered stale.
IG_CACHE_TTL_MINUTES=360

# Comma-separated list of allowed origins for the CORS policy on the HTTPS endpoint.
# Example: https://your-domain.com,https://www.your-domain.com
ALLOWED_ORIGINS=https://your-domain.com,http://localhost:3000
